我想设计两段提示词，根据一份季度报告生成一个可视化网页，第一段提示词负责提取核心内容和数据，第二段提示词负责生成网页 你帮我设计一下
第一段提示词：内容提取与结构化（给AI解析报告用）
**任务：** 请仔细阅读以下季度报告内容，并从中提取核心信息点和关键数据，将其组织成结构化的输出。目标是提供给下一阶段生成可视化网页使用。

**季度报告内容：**

[在这里粘贴或描述您完整的季度报告文本内容]
[如果报告是PDF/图片/网页，请提前转换为纯文本再粘贴。AI无法直接处理PDF/图片]

**提取要求：**

1.  **核心业绩指标（必选）：**
    *   识别并提取最重要的财务或业务指标（如：总收入、净利润、毛利润、每股收益、运营利润、现金流、成本/费用等）。
    *   提取这些指标的**实际值**和**同比/环比变化率（或数值）**（如Q3收入 $X.XM，同比增长 Y% / 环比增长 Z%）。
    *   标注清楚指标名称、季度（如 2024 Q3）、数值和单位。

2.  **关键亮点/成就（必选）：**
    *   提取报告内强调的 3-5 个最重要成就或积极进展（如：新产品发布成功、市场份额增长、关键客户签约、效率提升等）。
    *   尽量简洁，每个亮点用 1-2 句话概括。

3.  **主要挑战/风险（必选）：**
    *   提取报告内提到的主要挑战或潜在风险（如：供应链延迟、市场竞争加剧、成本压力、监管变化等）。
    *   同样每个点用 1-2 句话概括。

4.  **部门/业务线表现（如果适用）：**
    *   如果报告详细说明了不同部门或产品线的业绩，提取关键部门/产品线的表现和贡献。
    *   包括名称、增长率、收入占比等关键数据。

5.  **未来展望/指引（重要）：**
    *   提取公司对下一季度或财年的业绩预测、战略重点或关键行动项。

**输出结构要求：**

*   请将以上提取的信息**严格组织成 JSON 格式**。示例结构如下（请根据报告内容填充或调整结构）：
json

{

"report_info": {

"company": "[公司名称]",

"report_title": "[报告标题]",

"report_period": "[报告覆盖的季度，例如 2024 Q3]",

"as_of_date": "[报告发布日期，例如 2024-10-31]"

},

"key_metrics": [

{

"name": "总收入",

"value": "[数字]",

"unit": "[例如 USD/M, %, 等]",

"quarter": "[数值对应的季度]",

"yoy_change": "[同比变化率或数值，例如 15% 或 +$1.2M]",

"qoq_change": "[环比变化率或数值（如报告提供）]"

},

// ... 添加其他关键指标，如净利润、毛利率、EPS等

],

"highlights": [

"[亮点 1 的简要描述]",

"[亮点 2 的简要描述]",

// ...

],

"challenges": [

"[挑战 1 的简要描述]",

"[挑战 2 的简要描述]",

// ...

],

"segment_performance": [

{

"segment_name": "[部门/业务线名称]",

"revenue": "[收入数字]",

"growth": "[增长率]",

"notes": "[其他备注，如贡献率、原因等]"

},

// ... 其他部门/业务线

],

"outlook": [

"[展望/预测点 1]",

"[展望/预测点 2]",

// ...

]

}

*   **如果某些部分报告中没有涉及，请保留对应的JSON键但置为空数组 `[]` 或 `null`。**
*   请确保所有数值**准确**提取，避免丢失百分比符号`%`或货币单位`$`等关键信息（或者在JSON value中明确说明）。
*   对文字性描述进行**凝练总结**，保留核心意思。

**关键提示：**
*   仔细阅读报告，特别是数字表格部分和关键总结段落。
*   区分不同的时间点（本季度、上季度、去年同期）。
*   优先提取报告中着重强调和反复出现的数据点。

第二段提示词：生成可视化网页（给AI用第一段的输出生成HTML）
**任务：** 请使用下方提供的结构化数据（来自季度报告分析），创建一个独立的、可交互的HTML网页，用于直观展示报告的核心洞察和数据可视化。要求页面美观、专业且响应式。

**输入数据：**（请直接将第一段提示词的 *完整JSON输出结果* 粘贴在这里）
[将第一段提示词得到的 *完整JSON内容* 粘贴在此处！ 例如： `{ "report_info": {...}, "key_metrics": [...], ... }`]

**网页生成要求：**

1.  **页面结构 (HTML):**
    *   使用语义化标签 (`<header>`, `<main>`, `<section>`, `<footer>`)。
    *   **标题部分 (`<header>`):**
        *   清晰显示公司名称（`data.report_info.company`）和报告标题（`data.report_info.report_title`）。
        *   显示报告季度和发布日期（`data.report_info.report_period`, `data.report_info.as_of_date`）。
    *   **主体部分 (`<main>`):**
        *   分区域展示以下可视化内容：
            *   **核心指标概览区:** 突出展示 3-5 个最重要的 `key_metrics`（选择绝对值大或变化率显著的前3-5个）。使用卡片（Card）形式，每个卡片包含：指标名称、当前季度值（放大显示）、同比/环比变化（用绿色↑表示增长，红色↓表示下降）。数值变化旁动态显示百分比（例如 +15.2%）。
            *   **关键亮点 & 挑战区:** 分两个平行栏目展示 `highlights` 和 `challenges`，使用醒目的标题（例如 ✅ 关键成就 / ⚠️ 主要挑战）和简洁的项目符号列表。使用不同的背景色或图标区分。
            *   **指标趋势图表区（如果数据足够）:**
                *   创建一个时间序列折线图（`key_metrics`中至少包含两个不同时间点的数据才能生成图表），展示如“总收入”或“净利润”等核心指标在 *最近3-4个季度* 的变化趋势。X轴为时间（季度），Y轴为数值。**（数据不足则跳过此项）**
                *   或者/并且，创建柱状图展示各部门/业务的收入贡献 (`segment_performance.revenue`)。
            *   **部门/业务线表现区:** 如果 `segment_performance` 数据存在，创建一个饼图展示各业务线收入占比。或者使用表格展示各业务线的收入、增长率和备注。
            *   **未来展望区:** 清晰列出 `outlook` 中的要点。
    *   **样式要求 (CSS):** 内嵌在HTML的`<style>`标签中。
        *   使用现代、专业的配色方案（可参考公司Logo色或经典蓝白/灰）。
        *   字体：优先使用无衬线字体（如 `'Segoe UI', Tahoma, Geneva, Verdana, sans-serif`）。
        *   布局：清晰、留白合理。核心指标卡片、图表区域需要足够醒目。
        *   响应式设计：适配桌面和移动设备视图（使用媒体查询 `@media`）。
2.  **可视化库:** 使用 **ECharts 库 (v5.4.3)** 或 **Chart.js (v4.x)** 嵌入在页面中绘制图表。
    *   **确保提示词中包含正确的 ECharts CDN 链接或 Chart.js CDN 链接！**
    *   **选择说明：**
        *   **ECharts 示例 CDN (放在 `<head>`):**
html

<script src="https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js"></script>

*   **Chart.js 示例 CDN (放在 `<head>`):**
html

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>

*   在你的代码中选择**一种**并应用。图表配置需要美观清晰（标题、轴标签、图例、网格线等）。
3.  **交互性:**
*   对重要指标或图表的解释性文字，在鼠标悬停（`:hover`）时可以提供轻微高亮或提示。
*   图表本身应具有ECharts/Chart.js默认的交互功能（如悬停显示数据值、图例开关等）。
4.  **输出格式:**
*   生成一个**完整的、独立的、可直接在浏览器中打开的 HTML 文件内容**（包含`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`等完整结构）。
*   **所有CSS样式必须内嵌在`<style>`标签中。**
*   **所有JavaScript（包括图表初始化代码）必须内嵌在`<script>`标签中。**
*   利用输入的 JSON `data` 变量来填充页面内容和驱动图表数据（在JS中操作）。

**示例流程（伪代码思路，供你参考）：**

1.  在JS中定义变量 `const reportData = [粘贴进来的完整JSON数据];`。
2.  使用DOM操作或模板字符串，将 `reportData.report_info` 和文字性数据（highlights, challenges, outlook）填充到HTML对应位置。
3.  使用图表库（如ECharts）：
*   为每个图表准备一个具有明确ID的容器 `<div>` (如 `<div id="revenue-trend-chart" style="height: 400px;"></div>`)。
*   在`<script>`标签内，根据`reportData.key_metrics`或`reportData.segment_performance`的数据结构，配置对应类型的图表（折线图、柱状图、饼图）的 `option` 对象。
*   初始化图表实例并将其挂载到指定容器上：`const myChart = echarts.init(document.getElementById('revenue-trend-chart')); myChart.setOption(option);`
4.  确保数据映射正确（如提取哪几个季度的哪几个指标值放到图表中）。
5.  添加必要的CSS美化所有元素。

**注意：**
*   生成的HTML代码必须是功能完整且美观的。
*   特别注意图表数据的映射关系，确保使用来自输入 `reportData` 的正确字段路径。
*   使用内联样式和脚本，避免外部依赖（除了通过CDN引入的图表库）。
*   考虑移动端友好的尺寸和布局。
*   页面应加载顺畅，结构清晰。
重要提示
​​第一段输入准备：​​ 务必确保粘贴给第一段提示词的季度报告内容是​​高质量的纯文本​​。如果原始是PDF或图片，使用OCR或手动复制确保文本完整准确，尤其是数字和表格部分。
​​第一段输出验证：​​ 生成JSON后，务必​​仔细检查​​其准确性和完整性，特别是关键数据点、数值和单位。这是可视化质量的基础。如果第一段输出有错误或不完整，第二段生成的结果也不会好。
​​第二段输入：​​ ​​必须​​将第一段生成的​​完整JSON作为第二段提示词的核心输入​​。不要改动其结构（除非第一段确实提取有误）。
​​工具限制：​​ 当前最强大的AI模型（如GPT-4/Claude）在理解复杂表格和生成完美可视化代码方面仍有局限，尤其是在提供的数据或提示不清晰时。生成的网页代码可能需要人工​​微调和优化​​（调整CSS样式、修复图表配置、处理缺失数据等）。
​​图表库依赖：​​ 提示词中指定了 ECharts 或 Chart.js 的 CDN 链接，生成的 HTML 文件​​需要联网​​才能加载这些库以正常显示图表。
​​测试：​​ 将第二段生成的HTML代码保存为.html文件，并在不同浏览器和设备上打开测试，检查显示效果和功能是否符合预期。
通过这两段清晰分工的提示词，您可以高效地将一份原始的季度报告转化为一个生动、直观的数据可视化网页。祝您成功！

